{
  "name": "movie-library",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=7.10"
  },
  "scripts": {
    "start": "node src/server.js",
    "test": "NODE_ENV=test mocha src/**/*.spec.js",
    "test:acceptance": "NODE_ENV=test mocha src/**/*.accept.js",
    "pretest": "standard",
    "nodemon": "NODE_ENV=development nodemon --watch src -L src/server.js",
    "snyk-protect": "snyk protect",
    "prepublish": "npm run snyk-protect"
  },
  "dependencies": {
    "body-parser": "^1.17.1",
    "express": "^4.15.2",
    "mongodb": "^2.2.27",
    "morgan": "^1.8.1",
    "snyk": "^1.30.1",
    "verror": "^1.10.0"
  },
  "snyk": true,
  "devDependencies": {
    "chai": "^4.0.0",
    "dotenv": "^4.0.0",
    "gulp": "^3.9.1",
    "mocha": "^3.4.2",
    "run-sequence": "^1.2.2",
    "standard": "^10.0.2",
    "supertest": "^3.0.0"
  }
}
